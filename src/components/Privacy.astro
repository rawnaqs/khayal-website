<section class="privacy" id="privacy">
  <div class="container">
    <div class="privacy-content fade-in">
      <h2 class="section-label">Privacy</h2>
      <h3 class="privacy-title">Your data never<br />leaves your machine.</h3>
      <p class="privacy-desc">
        Not "privacy-friendly." Not "privacy-focused." Actually private. No servers, no accounts, no telemetry. Your vault is a folder on your disk — plain markdown files that you own completely.
      </p>
      <div class="privacy-features">
        <div class="pf-item fade-in fade-in-delay-1">
          <span class="pf-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </span>
          <div>
            <span class="pf-title">No cloud</span>
            <span class="pf-desc">Everything runs locally. No external network calls.</span>
          </div>
        </div>
        <div class="pf-item fade-in fade-in-delay-2">
          <span class="pf-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
          </span>
          <div>
            <span class="pf-title">No tracking</span>
            <span class="pf-desc">Zero analytics. Zero telemetry. We don't know you exist.</span>
          </div>
        </div>
        <div class="pf-item fade-in fade-in-delay-3">
          <span class="pf-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
          </span>
          <div>
            <span class="pf-title">Open source</span>
            <span class="pf-desc">AGPLv3. Every line of code is visible. Audit it yourself.</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .privacy {
    padding: 120px 32px;
    position: relative;
  }

  .privacy::before {
    content: '';
    position: absolute;
    bottom: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 500px;
    background: radial-gradient(ellipse, var(--gold-glow) 0%, transparent 60%);
    pointer-events: none;
  }

  .container {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .section-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--gold);
    margin-bottom: 20px;
    font-weight: 500;
  }

  .privacy-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 800;
    color: var(--text);
    line-height: 1.1;
    letter-spacing: -1.5px;
    margin-bottom: 20px;
  }

  .privacy-desc {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.7;
    margin-bottom: 48px;
    font-weight: 300;
  }

  .privacy-features {
    display: flex;
    flex-direction: column;
    gap: 16px;
    text-align: left;
  }

  .pf-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    padding: 20px 24px;
    background: var(--s2);
    border: 1px solid var(--border);
    border-radius: 12px;
    transition: border-color 0.2s;
  }

  .pf-item:hover {
    border-color: var(--border2);
  }

  .pf-icon {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: var(--gold-glow2);
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gold);
    flex-shrink: 0;
  }

  .pf-title {
    display: block;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 4px;
  }

  .pf-desc {
    font-size: 0.8rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .privacy { padding: 80px 20px; }
  }
</style>
